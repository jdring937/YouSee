<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:YouSee"
             x:Class="YouSee.MainPage">

    <Grid x:Name="grdButtonGrid">
        <!-- Set up grid rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="0*" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3.333*"/>
        </Grid.RowDefinitions>

        <!-- Set up grid columns-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".75*" />
            <ColumnDefinition Width=".75*" />
        </Grid.ColumnDefinitions>

        <!-- For full screen button -->
        <Button Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Text="▼" BackgroundColor="#ffc32b"></Button>

        <!-- For half screen buttons -->
        <Button x:Name="btnCreate" Grid.Column="0" Grid.Row="3" Text="Create" BackgroundColor="#ff3321"></Button>
        <Button x:Name="btnJoin" Grid.Column="1" Grid.Row="3" Text="Join" BackgroundColor="#ff3321"></Button>
    </Grid>

</ContentPage>
